export type Blog = {
    title: string
    date: string
    slug: string
}

export type Gig = {
    title: string
    venue: string
    address: string
    date: Date
    start: Date
    end: Date
    description: string
    image: string
}