export type Blog = {
    title: string
    date: string
    slug: string
}

export type Show = {
    title: string
    venue: string
    address: string
    date: Date
    start: string
    end: string
    description: string
    image: string
    slug: string
}