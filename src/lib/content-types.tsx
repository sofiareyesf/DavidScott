export type Blog = {
    title: string
    date: string
    slug: string
}

export type Gig = {
    title: string
    date: Date
    start: Date
    end: Date
    image: string
    description: string
    address: string
}